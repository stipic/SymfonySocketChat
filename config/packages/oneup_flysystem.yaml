oneup_flysystem:
    adapters:
        my_documents_adapter:
            local:
                directory: '%kernel.project_dir%/../public/%storage_dir%'
    filesystems:
        document:
            adapter: my_documents_adapter